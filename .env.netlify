# Variables d'environnement pour Netlify
# 
# ⚠️ IMPORTANT: Configurez ces variables dans l'interface Netlify
# Site settings > Environment variables > Add a variable
#
# OU via Netlify CLI:
# netlify env:set VARIABLE_NAME "value"

# ========================================
# VARIABLES CRITIQUES POUR LE FRONTEND
# ========================================

# URL de l'API Backend (Render)
# ⭐ CRITIQUE: Sans cette variable, le frontend ne peut pas communiquer avec le backend
VITE_API_URL=https://api.altusfinancegroup.com

# URL du site (pour SEO, canonical URLs, Open Graph)
VITE_SITE_URL=https://altusfinancegroup.com

# ========================================
# CONFIGURATION BUILD
# ========================================

# Version de Node.js (déjà dans netlify.toml)
NODE_VERSION=20

# ========================================
# NOTES
# ========================================
#
# 1. Les variables VITE_* sont accessibles dans le frontend via import.meta.env.VITE_*
# 2. Ne mettez JAMAIS de secrets backend ici (API keys, database URLs, etc.)
# 3. Ces variables sont publiques et visibles dans le code JavaScript final
# 4. Pour le backend sur Render, utilisez leur propre système de variables d'environnement
#
# ========================================
# VARIABLES À CONFIGURER SUR RENDER (Backend)
# ========================================
#
# Ces variables doivent être configurées sur Render, PAS sur Netlify:
# - DATABASE_URL (PostgreSQL connection string de Render)
# - SESSION_SECRET (généré avec: openssl rand -base64 32)
# - SENDGRID_API_KEY
# - SENDGRID_FROM_EMAIL
# - NODE_ENV=production
# - PORT=10000 (ou celui assigné par Render)
# - FRONTEND_URL=https://altusfinancegroup.com
# - CLOUDINARY_CLOUD_NAME
# - CLOUDINARY_API_KEY
# - CLOUDINARY_API_SECRET
#
# ========================================
