import { CometChatUIKit } from "@cometchat/chat-uikit-react";

export const initCometChat = async () => {
  console.log("✔️ CometChat will initialize on first login (server-managed)");
  return Promise.resolve();
};
